# VPC module — provisions VPC, public subnets, IGW, route table, and security groups.
# All-public subnet design (no NAT Gateway) — security groups are the perimeter. See ADR-0001.
# Implemented in: PETPLAT-6 (VPC + subnets), PETPLAT-8 (security groups).
