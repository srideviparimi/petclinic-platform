# DNS module — provisions Route 53 hosted zone and ACM wildcard certificate.
# Certificate validated via DNS (Route 53). Used by ALB Ingress for HTTPS termination.
# Implemented in: PETPLAT-28 (hosted zone + ACM), PETPLAT-31 (DNS alias record to ALB).
